<h1>Create a new tag</h1>

    <%= bootstrap_form_for @tag do |f| %>
    <%= f.text_field :name, :placeholder =>"Your new tag" %>
    <%= f.submit "Create a Tag", class: "btn btn-info" %>
    <% end %>

